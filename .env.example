# Optional. If you leave this empty, the agent runs in LOCAL MODE
# (extractive answers, no external API calls, works fully offline).
# If you add a key, the agent uses a real LLM to generate answers.
OPENAI_API_KEY=
